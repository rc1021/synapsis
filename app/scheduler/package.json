{
  "name": "synapsis-scheduler",
  "version": "1.0.0",
  "description": "Cron daemon for synapsis: runs scheduled jobs (shell + claude)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "node-cron": "^3.0.3"
  }
}
